import QtQuick 2.0

Item {
    id: editEntryScreen

    Component.onCompleted: {
        // XXX Populate the list of keys we can edit.
    }

    Rectangle {
        anchors.fill: parent
        color: "white"

        // XXX FINISH!
    }
}
